<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <%= link_to "BugTracker", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav ml-auto">
      <!--<li class="nav-item active">-->
        <!--<a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>-->
      <!--</li>-->
      <li class="nav-item <%= 'active' if current_page?(root_path) %>">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
      
      <li class="nav-item <%= 'active' if current_page?(projects_path) %>">
        <%= link_to "My Projects", projects_path, class: "nav-link" %>
      </li>
      <li class="nav-item <%= 'active' if current_page?(new_project_path) %>">
        <%= link_to "New project", new_project_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link logout-link" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "nav-link login-link" %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>